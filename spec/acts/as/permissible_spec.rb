RSpec.describe Acts::As::Permissible do
  it "has a version number" do
    expect(Acts::As::Permissible::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
